SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=

# Optional: default additionalMounts for Slack groups auto-registered on bot mention.
# JSON array matching the AdditionalMount schema in src/types.ts. Paths must be
# under an allowed root in ~/.config/nanoclaw/mount-allowlist.json.
# SLACK_DEFAULT_ADDITIONAL_MOUNTS=[{"hostPath":"/abs/path/to/repo","containerPath":"repos","readonly":false}]
